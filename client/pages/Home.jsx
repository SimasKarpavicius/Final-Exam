import styled from "styled-components";

const Container = styled.div`
  max-width: 1100px;
  margin: 3rem auto;
`;

const Home = () => {
  return <Container>Home</Container>;
};

export default Home;
